spring:
  #h2:
  #  console:
  #    enabled: true
  #    path: /h2-console
  #    settings:
  #      web-allow-others: true
  datasource:
    #url: jdbc:h2:mem:maindb2
    #driverClassName: org.h2.Driver
    #username: sa
    #password:

    url: jdbc:postgresql://localhost:5433/JavaProject
    username: postgres
    password: secret
    #driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    separator: ^
  sql:
    init:
      mode: always
      #mode: never
      #data-locations: data.sql
      #schema-locations: schema.sql
  jpa:
    hibernate:
      ddl-auto: none
    #database-platform: org.hibernate.dialect.H2Dialect
    #open-in-view: false
    generate-ddl: false

    show-sql: true
  main:
    allow-circular-references: true
  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: always
  kafka:
    consumer:
      group-id: "task-group"
      bootstrap-servers: "localhost:9092"
      client-id: "task-consumer"
      auto-offset-reset: earliest

  mail:
    host: localhost
    #port: 465
    port: 1025
    #username: 1Crom@mail.ru
    #password: Pkn6dXjMFmOAoBjWxd2t
    username: ""
    password: ""
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            #enable: true
            enable: false
        #мой настройка
        from: 1Crom@mail.ru




application:
  kafka:
    topic: "error-topic"


server:
  port: 8081


logging:
  level:
    ROOT: ERROR
    #ROOT: DEBUG
    ru.otus.hw.rest: DEBUG
    ru.otus.hw.services: DEBUG
    org.springframework.integration: INFO